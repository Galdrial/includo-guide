/**
 * TEST SETUP
 * Imports custom Jest-DOM matchers to extend Vitest's expect functionality.
 * Allows use of matchers like toBeInTheDocument(), toHaveClass(), etc.
 */
import '@testing-library/jest-dom';
